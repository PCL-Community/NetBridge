pluginManagement {
    repositories {
        // 阿里云镜像优先，官方插件仓库兜底
        maven { url = 'https://maven.aliyun.com/repository/public' }
        maven { url = 'https://maven.fabricmc.net' }
        maven { url = 'https://maven.neoforged.net' }
        gradlePluginPortal()
    }
}

rootProject.name = 'quic-mc'

include 'common'
include 'neoforge'
include 'fabric'
